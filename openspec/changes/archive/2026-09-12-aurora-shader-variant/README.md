# aurora-shader-variant

Design the aurora.shader variant interface: strong variants (macro/permutation language trimming) + weak variants (specialization constants), key->value selection to produce the final shader, and a reserved offline shader cache interface.
